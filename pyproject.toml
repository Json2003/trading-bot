[project]
name = "trading-bot"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "pandas>=2.0,<3",
  "numpy>=1.26,<3",
  "ccxt>=4.3,<5",
  "pydantic>=2.7,<3",
  "fastapi>=0.112,<1",
  "uvicorn>=0.30,<1",
  "python-dotenv>=1.0,<2",
  # add: ta, plotly/matplotlib, scipy, requests, backoff, tenacity, etc. as actually used
]
[project.optional-dependencies]
dev = ["pytest", "pytest-cov", "mypy", "ruff", "bandit", "pip-audit"]
