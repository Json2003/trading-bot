# Alternative requirements with robust network handling
# This file provides the same dependencies as requirements.txt but with
# additional configuration for environments with network connectivity issues

# Core trading libraries - current working versions
ccxt==3.0.72
pandas==2.2.2

# Alternative versions (if network issues require fallback)
# ccxt==4.3.35  # Requested version - uncomment if needed
# pandas==1.5.3  # Requested version - uncomment if needed

# Interactive Brokers integration
ib_insync==0.9.86

# Environment and configuration
python-dotenv==1.0.1

# Numerical computing
numpy==2.2.4

# Web framework
Flask==2.2.5

# Machine learning and data processing
river==0.11.0
scikit-learn==1.4.2
joblib==1.3.2

# GUI and desktop integration
pywebview==3.8
pystray==0.21.4
Pillow==10.1.0

# Windows-specific (install only on Windows)
pywin32==306; sys_platform == "win32"

# Progress and utilities
tqdm==4.67.1

# Additional trading APIs
python-binance==1.0.16

# Cloud storage
google-cloud-storage==2.11.0

# HTTP requests
requests==2.31.0

# Data formats
pyarrow==12.0.0

# Date handling
python-dateutil==2.8.2

# Web scraping
beautifulsoup4==4.12.2

# Installation notes:
# 1. Use install_dependencies.sh for robust installation with retries
# 2. If network issues persist, try: pip install -c pip.conf -r requirements-robust.txt
# 3. For air-gapped environments, consider using pip wheel and --find-links